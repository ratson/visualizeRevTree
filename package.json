{
  "name": "visualize-rev-tree",
  "version": "0.0.1",
  "repository": "https://github.com/ratson/visualizeRevTree",
  "devDependencies": {
    "pouchdb": "^5.0.0"
  },
  "scripts": {
    "build": "browserify --debug index.js > dist/index.js",
    "watch": "watchify index.js --debug -v -o dist/index.js"
  }
}
